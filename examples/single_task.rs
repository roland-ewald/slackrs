
use slackrs::{slack, plot::PlotTask, slack::MessageInChannel};

fn main() {
    let tasks: Vec<PlotTask> = vec![];

    //TODO let messages: Vec<MessageInChannel> = slack::read_zip_contents(&args.input_file);
    //TODO let _ = slackrs::process_tasks(&tasks, &messages);
}
